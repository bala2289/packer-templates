pipeline {
    agent any
    stages {
        stage('pipeline start') {
            steps {
                sh 'echo "pipeline initiated.."'
            }
        }
        stage('validate cloning') {
            steps {
                sh 'pwd;ls'
            }
        }
    }
}